@using crm_dotnet.Models.entity
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    TicketDepense[] ticketDepenses = (TicketDepense[])ViewData["ticketDepenses"];
}
<div class="loader"></div>
    <div id="app">
        <div class="main-wrapper main-wrapper-1">
            @Html.Partial("_leftSideBar")
            <!-- Main Content -->
            <div class="main-content">
                <section class="section">
                    <div class="section-body">
                        <div class="row">
                            <div class="col-12">
                                <div class="card">
                                    <div class="card-header">
                                        <h4>Details de ticket depense</h4>
                                    </div>
                                    <div class="card-body">
                                        <div class="table-responsive">
                                            <table class="table table-striped" id="table-1">
                                                <thead>
                                                <tr>
                                                    <th>#</th>
                                                    <th>ID Ticket</th>
                                                    <th>Subdget</th>
                                                    <th>Client</th>
                                                    <th>Employer assigner</th>
                                                    <th>Montant de depense</th>
                                                    <th>Date d'ajout de depense</th>
                                                    <th>Action</th>
                                                </tr>
                                                </thead>
                                                <tbody>
                                                    @foreach (var t in ticketDepenses)
                                                    {
                                                        <tr>
                                                            <td>@t.Id</td>
                                                            <td>@t.Ticket_id</td>
                                                            <td>@t.Ticket.Subject</td>
                                                            <td>@t.Ticket.Customer.Name</td>
                                                            <td>@t.Ticket.Employee.Username</td>
                                                            <td>@t.Montant</td>
                                                            <td>@t.Date_ajout</td>
                                                            <td>
                                                                <a asp-controller="Dashboard" asp-action="DeleteTicket" asp-route-id="@t.Id" class="btn btn-danger"><i class="fa fa-trash"></i></a>
                                                                <a asp-controller="Dashboard" asp-action="ShowFormUpdateTicket" asp-route-id="@t.Id" class="btn btn-danger"><i class="fa fa-edit"></i></a>
                                                            </td>
                                                        </tr>
                                                    }
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
        </div>
    </div>